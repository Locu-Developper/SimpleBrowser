<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MemoBrowser.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MemoBrowser"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="MemoBrowser">

    <Grid KeyDown="common_KeyDown">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBox    Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" PlaceholderText="url or word" x:Name="addressBar" KeyDown="addressBar_KeyDown"/>
        <Button     Grid.Row="0" Grid.Column="1" Click="SearchingButton_Click">検索</Button>
        <WebView2   Grid.Row="1" Grid.Column="0" x:Name="webView" Source="https://google.com" />
        <ListView Grid.Row="1" Grid.Column="1" ItemsSource="{x:Bind tabViewModel.Tabs}">
            <ListView.KeyboardAccelerators>
                <KeyboardAccelerator Key="T" Modifiers="Control" Invoked="Add_Tab"/>
            </ListView.KeyboardAccelerators>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:TabNode">
                    <TextBlock Text="{x:Bind Url}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
